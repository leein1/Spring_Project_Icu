<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="kr.liw.mapper.LikesMapper">
	  
	<insert id="insertLike">
		insert into ICU_LIKES (bno,email,status,boardtype)
		VALUES(#{bno}, #{email}, 'like', #{boardtype})
	</insert>
	
	<delete id="deleteLike">
		delete from ICU_LIKES where BOARDTYPE = #{boardtype}  and BNO = #{bno} and EMAIL = #{email} 
	</delete>
	
	<select id="checkLike" resultType="int">
		select COUNT(*) from ICU_LIKES where BOARDTYPE = #{boardtype}  and BNO = #{bno} and EMAIL = #{email} 
	</select>
	
	<select id="countLike" resultType="int">
		SELECT COUNT(*) FROM ICU_LIKES WHERE BOARDTYPE = #{boardtype} AND BNO = #{bno} AND status = 'like'
	</select>

</mapper>